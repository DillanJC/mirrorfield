# PLAN — auto session, 2026-07-04

**Handover:** Dillan (at work): "keep working on things and if there is nothing left make
future plans and start work on those too." Operating under `CLAUDE.md` hard stops and the
state-doc §4 auto-mode rules. Forum: 7–8 July (3–4 days out).

## Units (small, reversible, each committed + logged in AUTO_LOG.md)
1. **Auto-mode kit** — this file, `AUTO_LOG.md`, lean `CLAUDE.md` (the state doc assumes they exist; they didn't).
2. **Forum walk-in kit draft** (`docs/FORUM_KIT.md`) — 60-second story, orientation paragraph, Murfet-question options. Dillan's voice/edit; nothing shared.
3. **Repo hygiene** — stale-install shadow check (uninstall pre-authorized, state doc §6/§9); legacy-roadmap banner check.
4. **Persistence fix** (state doc §9 safe-list #1) — raw mm/me/br (+ task labels) persisted by default in row-saving. Code-read verified only — no GPU in auto; flagged as run-unverified.
5. **Temperature-scaled baseline** (state doc §6 methodology item) — CPU-only, saved rows, cross-seed held-out; amendment committed BEFORE analysis; candidate verdict only. Scoped as a *diagnostic baseline*: the deployed gate is untouched; the intervention-level refit stays parked per §7.
6. **Second-model transfer protocol DRAFT** (`plans/I-second-model-transfer.md`) — NOT locked; GPU run waits for Dillan.
7. **HANDOFF.md update** — session summary for Dillan's return audit.

## Explicitly not doing
GPU generation of any kind; the framing benchmark (unlocked, GPU); the deployment refit;
anything geometry; anything public; concluding any verdict.
